# Custom Rules

- **Git Commit and Push**: Whenever the user asks to commit and/or push, stage all modified files, commit them, and always push the changes to all configured git remotes (e.g. `origin` and `cloned`) to keep them fully in sync.
